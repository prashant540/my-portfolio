<h1>Portfolio Items</h1>
<%= link_to "Create New Portfolio",new_portfolio_path%>
<%@portfolios.each do |item|%>
<p><%=item.title%></p>
<p><%=item.subtitle%></p>
<p><%=item.body%></p>
<%= link_to "Edit this portfolio","/portfolios/#{item.id}/edit"%>
<%= image_tag item.thumb_image if item.thumb_image.present?%>
<%end%>